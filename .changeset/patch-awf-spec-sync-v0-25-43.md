---
"gh-aw": patch
---

Sync embedded AWF config schema with the latest `gh-aw-firewall` spec updates and start emitting `apiProxy.modelMultipliers` in generated AWF config when `engine.token-weights.multipliers` is configured.
